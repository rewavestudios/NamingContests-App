module.exports = {
    presets: [
        ['@babel/preset-react',
        //{modules: false}
        ],
        '@babel/preset-env'
    ],
    plugins: [
        '@babel/plugin-proposal-class-properties'
    ],
};